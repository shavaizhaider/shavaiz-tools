[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "shavaiz-tools"
version = "1.0.0"
description = "A simple Python utility library"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["shavaiz_tools"]